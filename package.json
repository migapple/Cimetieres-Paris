{
  "name": "cimetieres-paris",
  "version": "1.0.0",
  "description": "Tombes célèbres - Père-Lachaise & Montparnasse",
  "scripts": {
    "build": "echo 'No build step needed — pure HTML/JS'",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open ios",
    "cap:run": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
